#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ifstream file;
    file.open("sample.txt");  // open file for reading

    string text;
    while (getline(file, text)) {
        cout << text << endl;
    }

    file.close();
    return 0;
}
